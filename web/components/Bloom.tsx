// Decorative violet bloom — pure CSS, 0 KB, single composited layer.
// See DASHBOARD-BRIEF.md §1 (bloom) and §8 (motion budget).

export function Bloom() {
  return <div className="bloom" aria-hidden="true" />;
}
